@if (!ViewContext.ViewData.ModelState.IsValid)
{
    <div class="panel panel-danger">
        <div class="panel-heading">
            <h3 class="panel-title">
                با توجه به اینکه می خواید اطلاعات ملک به صورت عمومی روی اینترنت باشه، به دلیل خطاهای زیر نمی تونیم ثبت رو کامل کنیم
            </h3>
        </div>
        <div class="panel-body">
            @Html.ValidationSummary()
        </div>
        <div class="panel-footer">
            لطفاً برای ثبت اطلاعات ملک این موارد رو رفع کنید، یا اینکه اطلاعات رو به صورت «غیر عمومی» ثبت کنید
        </div>
    </div>
}
